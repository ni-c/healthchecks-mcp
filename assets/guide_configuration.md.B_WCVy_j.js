import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"Configuration","description":"","frontmatter":{},"headers":[],"relativePath":"guide/configuration.md","filePath":"guide/configuration.md","lastUpdated":1788283385000}`),a={name:`guide/configuration.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to “Configuration”">​</a></h1><p>See the <a href="/reference/environment">environment variable reference</a> for the full table.</p><h2 id="the-api-key" tabindex="-1">The API key <a class="header-anchor" href="#the-api-key" aria-label="Permalink to “The API key”">​</a></h2><p><code>HEALTHCHECKS_API_KEY</code> is a <strong>project</strong> API key from Project Settings → API Access, exactly 32 characters long. Keys do not span projects: a key from project A answers <code>403</code> for a check in project B, and because the API looks the object up globally before checking ownership, that <code>403</code> means &quot;right UUID, wrong key&quot; far more often than it means &quot;not allowed&quot;.</p><p>The key is read once and then deleted from <code>process.env</code>, so it is not visible to child processes or in <code>/proc/&lt;pid&gt;/environ</code>, and it is never placed in a request body — the API would accept it there on POST, and it would end up in access logs.</p><h2 id="read-only-and-read-write-keys" tabindex="-1">Read-only and read-write keys <a class="header-anchor" href="#read-only-and-read-write-keys" aria-label="Permalink to “Read-only and read-write keys”">​</a></h2><p>Every project can issue both. The difference reaches further than it looks:</p><table tabindex="0"><thead><tr><th>With a read-only key</th><th>Consequence</th></tr></thead><tbody><tr><td>checks carry no <code>uuid</code>, only a 40-char <code>unique_key</code></td><td><code>get_check</code> and <code>list_flips</code> accept either; nothing else can be addressed</td></tr><tr><td>no <code>ping_url</code>, <code>update_url</code> or <code>channels</code></td><td>you cannot see where a check is pinged</td></tr><tr><td><code>list_pings</code>, <code>get_ping_body</code>, <code>list_integrations</code> are rejected</td><td>with <code>401 &quot;wrong api key&quot;</code>, even though all three only read — the tools translate that into the real reason</td></tr><tr><td>every write tool is rejected</td><td>pair it with <code>HEALTHCHECKS_READ_ONLY=true</code> so they are not offered</td></tr></tbody></table><p><code>get_api_key_info</code> performs two harmless GETs and tells you which kind you have, which identifier your checks will use, and exactly which tools are out of reach. Run it first when something answers <code>401</code> that you expected to work.</p><h2 id="read-only-mode" tabindex="-1">Read-only mode <a class="header-anchor" href="#read-only-mode" aria-label="Permalink to “Read-only mode”">​</a></h2><p><code>HEALTHCHECKS_READ_ONLY=true</code> does not register the five write tools at all. They are absent from <code>tools/list</code> and unknown to <code>tools/call</code> — not refused at call time, because advertising a capability and then declining it is worse than not advertising it.</p><p>This is the right setting whenever the model is meant to answer questions rather than change monitoring. It also pairs naturally with a read-only API key: without it, a model can see <code>delete_check</code>, try it, and get an opaque authorisation error instead of a clear absence.</p><h2 id="turning-the-approval-dialog-off" tabindex="-1">Turning the approval dialog off <a class="header-anchor" href="#turning-the-approval-dialog-off" aria-label="Permalink to “Turning the approval dialog off”">​</a></h2><p><code>delete_check</code> asks a person through MCP elicitation before it acts. <code>ELICITATION=false</code> takes it to the two-call token instead. It does not remove the guard; there is no setting in which a guarded call goes unannounced.</p><p>The variable deliberately carries no <code>HEALTHCHECKS_</code> prefix, which means it reaches every MCP server in the same environment, and — unlike the booleans above — a value it does not recognise <strong>stops the server</strong> rather than failing open. See <a href="/guide/approval">Asking a person</a>.</p><h2 id="tls-and-self-hosted-instances" tabindex="-1">TLS and self-hosted instances <a class="header-anchor" href="#tls-and-self-hosted-instances" aria-label="Permalink to “TLS and self-hosted instances”">​</a></h2><p><code>HEALTHCHECKS_URL</code> must be <code>http://</code> or <code>https://</code>, must carry no <code>user:pass@</code>, and is trimmed to the site root. Over plain <code>http</code> to anything but a loopback address the server prints a warning: the API key travels unencrypted there.</p><p><code>HEALTHCHECKS_INSECURE_TLS=true</code> accepts a self-signed certificate. It is scoped to this server&#39;s own connections through a dedicated undici dispatcher — it never touches <code>NODE_TLS_REJECT_UNAUTHORIZED</code>, which would disable verification for the whole process. Prefer putting your internal CA in the trust store; this switch is for the afternoon when that is not yet true.</p><h2 id="choosing-the-tools-that-load" tabindex="-1">Choosing the tools that load <a class="header-anchor" href="#choosing-the-tools-that-load" aria-label="Permalink to “Choosing the tools that load”">​</a></h2><p>Read-only mode is one cut, along a line this server drew for you. <code>HEALTHCHECKS_ALLOW_TOOLS</code> and <code>HEALTHCHECKS_DENY_TOOLS</code> let you draw your own:</p><div class="language-sh"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light-default github-dark-default" style="--shiki-light:#1f2328;--shiki-dark:#e6edf3;--shiki-light-bg:#ffffff;--shiki-dark-bg:#0d1117;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">HEALTHCHECKS_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">essential</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">HEALTHCHECKS_ALLOW_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">list_*,get_check</span></span>
<span class="line"><span style="--shiki-light:#1F2328;--shiki-dark:#E6EDF3;">HEALTHCHECKS_DENY_TOOLS</span><span style="--shiki-light:#CF222E;--shiki-dark:#FF7B72;">=</span><span style="--shiki-light:#0A3069;--shiki-dark:#A5D6FF;">delete_check,pause_check</span></span></code></pre></div><p>Why bother, when all of them work: a model chooses the right tool far more reliably from a handful than from a long list, and every tool it can see costs context on every single request. If this is the only MCP server in a session, the full set is fine. If it is one of six, it is not.</p><p><strong>The syntax.</strong> Comma-separated entries. An entry is either an exact tool name or a prefix with a trailing <code>*</code> — <code>list_*</code> matches every tool whose name starts with <code>list_</code>. Entries are trimmed and case-insensitive, empty ones are ignored, and an empty value counts as unset. Nothing else is a pattern: <code>*_thing</code> and <code>list_*_x</code> are rejected rather than silently matching nothing.</p><p><strong><code>essential</code></strong> is a curated preset: <code>list_checks</code>, <code>get_check</code>, <code>list_pings</code>, <code>list_flips</code>, <code>create_check</code>, <code>update_check</code> and <code>resume_check</code>. It is marked per tool in the <a href="/reference/tools">tool reference</a>, and a test compares those markers against the same constant the filter reads, so the two cannot drift. It composes — naming a tool alongside it puts that one back, and <code>HEALTHCHECKS_DENY_TOOLS</code> takes one away.</p><p><strong>Both together.</strong> <code>HEALTHCHECKS_ALLOW_TOOLS</code> decides what is in; <code>HEALTHCHECKS_DENY_TOOLS</code> is then subtracted from the result. With only a deny list, everything else stays.</p><p><strong>A name that matches nothing stops the server</strong>, with the offending entry and the list of real names. That is deliberate: the alternative is a tool quietly missing from <code>tools/list</code>, and nobody traces an absence back to an environment variable. The same applies to a pattern that matches no tool.</p><p><strong>With read-only mode</strong>, the write tools are not registered at all, so naming one explicitly in <code>HEALTHCHECKS_ALLOW_TOOLS</code> is an error that says so — rather than calling a tool unknown when it plainly exists. A <em>pattern</em> that covers write tools is fine and simply contributes nothing, which is what makes <code>get_*,create_*</code> a usable template for both kinds of deployment; and <code>HEALTHCHECKS_ALLOW_TOOLS=essential</code> narrows to the read half of the preset.</p><div class="tip custom-block"><p class="custom-block-title">It is the same cut, not a second one</p><p>A filtered tool is never registered, so it is absent from <code>tools/list</code> and unknown to <code>tools/call</code> alike — exactly what <code>HEALTHCHECKS_READ_ONLY</code> does to a write tool. There is no &quot;hidden but callable&quot; state to reason about.</p></div>`,28)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};