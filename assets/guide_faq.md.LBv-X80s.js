import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.Cs5aHXsR.js";var i=JSON.parse(`{"title":"FAQ & troubleshooting","description":"","frontmatter":{},"headers":[],"relativePath":"guide/faq.md","filePath":"guide/faq.md","lastUpdated":1787824128000}`),a={name:`guide/faq.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="faq-troubleshooting" tabindex="-1">FAQ &amp; troubleshooting <a class="header-anchor" href="#faq-troubleshooting" aria-label="Permalink to “FAQ &amp; troubleshooting”">​</a></h1><h2 id="one-tool-i-expected-is-missing" tabindex="-1">One tool I expected is missing <a class="header-anchor" href="#one-tool-i-expected-is-missing" aria-label="Permalink to “One tool I expected is missing”">​</a></h2><p>Something narrowed the list. In order of likelihood:</p><ul><li><code>HEALTHCHECKS_READ_ONLY</code> is set, and it is a write tool.</li><li><code>HEALTHCHECKS_ALLOW_TOOLS</code> is set and does not name it — it is an allow list, so anything not named is out.</li><li><code>HEALTHCHECKS_DENY_TOOLS</code> names it, possibly through a prefix such as <code>delete_*</code>.</li></ul><p>A filtered tool is not registered at all, so it is missing from <code>tools/list</code> and answers <code>tools/call</code> with &quot;tool not found&quot; — the same as a write tool under read-only. There is no state where it is hidden but still callable.</p><p>What it is <em>not</em> is a typo in one of those variables: an entry that matches no tool stops the server at startup and says which entry it was. See <a href="/guide/configuration#choosing-the-tools-that-load">choosing the tools that load</a>.</p><h2 id="list-checks-says-the-project-is-empty-but-it-is-not" tabindex="-1"><code>list_checks</code> says the project is empty, but it is not <a class="header-anchor" href="#list-checks-says-the-project-is-empty-but-it-is-not" aria-label="Permalink to “list_checks says the project is empty, but it is not”">​</a></h2><p>Two causes, in order of likelihood.</p><p><strong>Something in front of the instance answered instead of the API.</strong> An SSO portal, a captive proxy or a login page replies <code>200 text/html</code> to <code>/api/v3/checks/</code>. The server refuses that rather than reading it as an empty list — an answer of &quot;you have no checks&quot; would be an error dressed up as data — so you get a message naming the content type it got. Check <code>HEALTHCHECKS_URL</code>, and try <code>get_status</code>, which needs no API key.</p><p><strong>The key belongs to a different project.</strong> API keys are per project, not per account, so a key for project A genuinely sees no checks from project B. Run <code>get_api_key_info</code>: it reports which instance is configured and whether the key is accepted at all.</p><h2 id="everything-answers-401-but-the-key-is-right" tabindex="-1">Everything answers 401, but the key is right <a class="header-anchor" href="#everything-answers-401-but-the-key-is-right" aria-label="Permalink to “Everything answers 401, but the key is right”">​</a></h2><p>Healthchecks validates the key by <strong>length</strong> before it looks it up, so a key that is not exactly 32 characters produces <code>401 missing api key</code> — which reads as if no header was sent. This server checks the length itself and says so at startup and on the first call, but if you are looking at a raw 401 from somewhere else, count the characters first.</p><p>The other half of the same confusion, and the more common one: <code>list_pings</code>, <code>get_ping_body</code> and <code>list_integrations</code> require a <strong>read-write</strong> key even though they only read. A read-only key is refused there with <code>401 {&quot;error&quot;: &quot;wrong api key&quot;}</code> — the same status and the same wording as a key that does not exist, because the API cannot distinguish &quot;not allowed&quot; from &quot;not a key&quot; at that point.</p><p>Those three tools translate it rather than passing it on, so you get a sentence saying the key is read-only instead of one implying it is broken. <code>get_api_key_info</code> lists exactly which tools are out of reach.</p><h2 id="why-can-it-not-ping-a-check" tabindex="-1">Why can it not ping a check? <a class="header-anchor" href="#why-can-it-not-ping-a-check" aria-label="Permalink to “Why can it not ping a check?”">​</a></h2><p>Deliberately. Pinging is a job&#39;s statement that it ran, and this same server hands the model ping bodies written by whatever pings the check — a ping tool would close a loop from untrusted text to falsified monitoring. Ping URLs are also authenticated by a separate ping key that the Management API does not expose. See <a href="/guide/security#what-the-key-cannot-do">Security</a>.</p>`,16)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};